# Tom Distributed Orchestration Tool

> **Status: placeholder.** This README was the one `dart create` emits, and it
> described an entry point in `bin/` that this package does not have — `lib/src/`
> holds only the generated versioner. No `orchestrator` command is built from
> it, and it has been removed from the workspace tool table
> (`_copilot_guidelines/tool_dependencies.md`, "Retired tools").

Library code lives in `lib/`, with an example unit test in `test/`.
