name: tom_process_monitor_tool
description: CLI tools for ProcessMonitor - process_monitor and monitor_watcher commands.
version: 1.0.0
repository: https://github.com/al-the-bear/tom_module_distributed
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  path: ^1.9.0
  args: ^2.5.0
  tom_process_monitor:
    path: ../tom_process_monitor

executables:
  process_monitor: process_monitor

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
